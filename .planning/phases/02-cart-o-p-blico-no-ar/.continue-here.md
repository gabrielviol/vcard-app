---
context: phase
phase: 02-cart-o-p-blico-no-ar
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-08-15T16:00:40.819Z
---

<current_state>
Phase 2 (Cartão Público no Ar), Wave 2 is done except plan 02-05. Wave 3 (02-06, domain/deploy
cutover) is blocked on 02-05 finishing. 02-05's Task 1 (checkpoint:human-action, gate="blocking")
is where we're paused: it needs the user to provision Neon/Render/Vercel/cron-job.org, but the
user chose to DEFER domain registration (BRAND-01) rather than do it now.
</current_state>

<completed_work>

Wave 1 (merged, tested):
- Task 02-01: Public `GET /public/cards/{slug}` endpoint + `/[slug]` Server Component page (ISR 60s) — Done
- Task 02-02: `app/[slug]/qr/route.ts` — inline SVG + `?download=1` + `?format=png` — Done

Wave 2 (merged, tested):
- Task 02-03: Branded 404 (`not-found.tsx`) + error boundary (`error.tsx`) — Done. Task 2's
  human-verify checkpoint was resolved with real `curl -I` evidence from the user:
  `/slug-que-nao-existe` → `404 Not Found`, `/login` → `200 OK`. This closed the STATE.md
  blocker on static-route-vs-`/[slug]` precedence. Steps 1/2/3/5/7/8 (browser/DOM steps) were
  approved by user sign-off WITHOUT individually captured evidence — recorded honestly in
  02-03-SUMMARY.md, not fabricated.
- Task 02-04: QR shown permanently in dashboard (SVG/PNG download) + fire-and-forget prewarm
  fetch on card save (wakes Vercel → Render → Neon) — Done, 76/76 tests green.

Post-merge gates after every wave: xUnit (95/95), Vitest (76/76), tsc --noEmit clean, eslint clean.
</completed_work>

<remaining_work>

- **02-05 Task 1** (blocking checkpoint, in progress): needs from the user —
  1. Neon connection string (with `SSL Mode=Require;Trust Server Certificate=true`)
  2. Confirmation Render account exists + connected to this repo
  3. Confirmation Vercel account (from Phase 1 Blob usage) still accessible
  4. Confirmation cron-job.org account exists
  Domain registration (item 1 in the original plan) is DEFERRED — do not ask for it again unless
  the user brings it up. See `<decisions_made>`.
- **02-05 Task 2** (not started): Dockerfile (multi-stage, `mcr.microsoft.com/dotnet/sdk:10.0` →
  `aspnet:10.0`), `.dockerignore`, apply EF Core migrations to Neon, write `docs/DEPLOY.md` runbook.
- **02-05 Task 3** (not started): Create Render Web Service, fill env vars, verify `/health`
  (200, `database: up`) and `/public/cards/slug-que-nao-existe` (404, not 401), create cron-job.org
  cronjob at 5 min interval, verify ≥5 consecutive 200s.
- **02-06** (not started, depends on 02-05): Deploy `apps/web` to Vercel, point the (eventually
  registered) domain at it, set `NEXT_PUBLIC_APP_URL` to the real value — zero code changes needed
  since the URL was never hardcoded (D-15).
</remaining_work>

<decisions_made>

- **Defer BRAND-01 domain registration.** User was unfamiliar with the registration process and
  wanted to avoid that friction/cost right now. Confirmed this is safe: nothing in 02-05 or 02-06
  technically requires the domain — `Cors__WebOrigin` and `NEXT_PUBLIC_APP_URL` both explicitly
  support provisional `*.vercel.app`/`*.onrender.com` values in the plan text itself, with the real
  domain swapped in later at zero code cost. The plan's own acceptance criteria already allow for
  "registration failed/deferred → document and escalate as a STATE.md blocker" instead of treating
  it as a hard stop. Do NOT re-litigate this decision on resume — just proceed with the 4 accounts
  above and leave the domain as an explicitly open, non-blocking item.
</decisions_made>

<blockers>
- 02-05 Task 1: waiting on user to create the 4 accounts and send back credentials (see remaining_work). No workaround — genuinely needs the user, no CLI/API access exists for these with the required credentials.
</blockers>

## Required Reading (in order)
1. `.planning/phases/02-cart-o-p-blico-no-ar/02-05-PLAN.md` — full plan text, all 3 tasks, exact env var names Program.cs reads eagerly (`ConnectionStrings__Default`, `JWT_SECRET`, `Jwt__Issuer`, `Jwt__Audience`, `Cors__WebOrigin`)
2. `.planning/STATE.md` — Blockers/Concerns section, for the route-precedence resolution note and to confirm domain deferral hasn't already been logged there yet (add it on resume if missing)
3. `.planning/phases/02-cart-o-p-blico-no-ar/02-CONTEXT.md` D-14/D-15 — domain naming candidates, in case the user comes back ready to register

## Infrastructure State
- Local: `docker compose up -d` (Postgres on 5432) + `dotnet run --project apps/api` (needs `ConnectionStrings__Default` etc. exported manually — .NET does not auto-load `.env`, see PowerShell snippet used earlier in session) + `apps/web` (`npm run dev` or `build && start`) — all working as of this pause.
- No production infra exists yet. Neon/Render/cron-job.org accounts not yet created by user.
- Residual (non-blocking) leftover worktree directories on disk under `.claude/worktrees/` from earlier in this session and prior sessions — git no longer tracks them as worktrees (`git worktree list` is clean), but the directories are permission-locked and couldn't be deleted. Safe to ignore; clean up manually later if desired.

<context>
Executing Phase 2 wave-by-wave via /gsd:execute-phase. Waves 1 and 2 (4 of 6 plans) are fully
merged, tested, and tracked in ROADMAP.md/STATE.md. 02-05 is the sole remaining wave-2 plan and
the sole thing blocking wave 3 (02-06, the final domain/deploy cutover that closes out the phase).
The user asked to pause here specifically to go do account-creation legwork (Neon/Render/Vercel/
cron-job.org) at their own pace, having decided to defer the domain purchase. This is a clean,
non-broken pause point — nothing failed, nothing is mid-edit.
</context>

<next_action>
Start with: When the user comes back with the Neon connection string and account confirmations,
first log the domain-deferral decision in STATE.md's Blockers/Concerns section if not already
there, then spawn a fresh `gsd-executor` agent (isolation="worktree", base off current master
HEAD) for plan 02-05 with the collected values pre-supplied in the prompt (same pattern used
earlier this session for the 02-02 package-approval retry) so it skips Task 1's checkpoint
entirely and proceeds directly to Task 2 (Dockerfile + migrations + docs/DEPLOY.md) and Task 3
(Render deploy + cron activation). Do not re-ask about the domain.
</next_action>
