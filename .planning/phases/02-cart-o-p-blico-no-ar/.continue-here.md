---
context: phase
phase: 02-cart-o-p-blico-no-ar
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-08-18T01:58:42.169Z
---

<current_state>
Phase 2 (Cartão Público no Ar), plan 02-06 (the last plan of the phase), Task 1
(`checkpoint:human-action`, gate="blocking"). We're mid-task: the Vercel project for
`apps/web` is imported and deployed, giving `https://vcard-app-one.vercel.app`. The
user was told to (1) set `NEXT_PUBLIC_APP_URL` to that URL on Vercel and redeploy,
(2) update `Cors__WebOrigin` on Render to that same origin and redeploy, (3) confirm
login+save works on the new URL with no CORS error. Session was paused before the
user confirmed those 3 steps were done.
</current_state>

<completed_work>

Wave 1 + 2 (merged, tested, pushed to origin/main): 02-01, 02-02, 02-03, 02-04 — see
prior SUMMARYs.

Plan 02-05 (merged, closed): backend `.NET` containerized and live on Render at
`https://vcard-app-tihd.onrender.com`, Postgres schema migrated to Neon production,
cron-job.org keep-alive active at 5 min (user-reported "um monte tudo status 200",
recorded honestly as informal confirmation, not a captured screenshot/count — same
pattern as 02-03's precedent). `docs/DEPLOY.md` created with Render setup + a
documented pitfall: Render's "Dockerfile Path" field is relative to "Root Directory",
so with Root Directory `apps/api`, Dockerfile Path must be just `Dockerfile` — setting
it to `apps/api/Dockerfile` again causes a double-path build error
(`lstat .../apps/api/apps: no such file or directory`), which the user actually hit
and we fixed live.

Plan 02-06 Task 1 (in progress): Vercel project imported, Root Directory `apps/web`,
deployed successfully. Got production URL `https://vcard-app-one.vercel.app`. Set
`NEXT_PUBLIC_API_URL=https://vcard-app-tihd.onrender.com` on Vercel already. User was
walked through, but has not yet confirmed:
- Setting `NEXT_PUBLIC_APP_URL=https://vcard-app-one.vercel.app` (no trailing slash)
  on Vercel + redeploying
- Setting `Cors__WebOrigin=https://vcard-app-one.vercel.app` on Render + redeploying
- Testing login + save at the new URL with dev tools open, confirming no CORS error
</completed_work>

<remaining_work>

- **02-06 Task 1** (in progress, blocking checkpoint): get the user's confirmation on
  the 3 items above. If CORS errors appear, the most likely cause is the Render env
  var not having actually redeployed yet (Render doesn't always auto-redeploy on env
  var save — may need a manual "Deploy latest commit").
- **02-06 Task 2** (not started, `type="auto"`): update `docs/DEPLOY.md` with real
  Vercel state (Root Directory, the 3 Production env vars, their sources) and a
  go-live checklist covering all 9 phase requirement IDs (PUB-01..06, SHARE-01/02,
  BRAND-01). **Adaptation needed**: the plan's Task 2 action text assumes a
  registered custom domain exists (a "DNS" section with real registro.br records, a
  "Trocar o domínio" section). Since no domain is registered, write the DNS section
  as "not yet applicable — BRAND-01 deferred" rather than fabricating DNS records,
  and keep the "Trocar o domínio" section as forward-looking procedure (it's still
  valid content — it describes what happens WHEN a domain gets added later). Do NOT
  mark BRAND-01 as done in the go-live checklist — mark it explicitly deferred/open.
- **02-06 Task 3** (not started, `type="checkpoint:human-verify"`, blocking): 12-step
  end-to-end verification. **Adapt every `{D}` (domain placeholder) in the plan text
  to `vcard-app-one.vercel.app`** instead of a custom domain. Step 1 (BRAND-01 TLS
  check against the custom domain) should be explicitly skipped/marked deferred
  rather than run against the Vercel URL as if it were the final domain — Vercel's
  own `*.vercel.app` TLS is real but is not what BRAND-01 is asking for. All other
  steps (2–12: public card render, minimal card, 404, route precedence, ISR timing,
  cold start timing, edit propagation, prewarm, QR on-screen, QR download/scan,
  Brazilian-accented name rendering) apply as written, just against the Vercel URL.
  Collect the literal curl outputs and timing numbers the acceptance_criteria
  requires before writing `02-06-SUMMARY.md`.
</remaining_work>

<decisions_made>

- **Proceed with plan 02-06 using the provisional `https://vcard-app-one.vercel.app`
  URL instead of a registered domain.** User was asked explicitly (AskUserQuestion,
  two options: deploy now with provisional URL vs. pause until domain is registered)
  and chose to proceed now. Explicit tradeoff acknowledged: any QR codes generated
  against this URL will need reprinting later if/when a real domain replaces it —
  this matches the plan's own "Trocar o domínio" section, which already documents
  regenerating QR codes as the real cost of a domain swap. Do NOT re-litigate this on
  resume; if the user brings up the domain, proceed per this decision, don't re-ask
  which path to take.
- Same-session precedent reused: gsd-executor spawned with `isolation="worktree"`,
  fast-forward merged into `main`, worktree+branch cleaned up, then pushed to
  `origin/main`. This is the established pattern for `type="auto"` tasks in this
  phase (used twice for 02-05's Task 2 and Task-3-closeout) — reuse it for 02-06
  Task 2.
</decisions_made>

<blockers>
- 02-06 Task 1: waiting on user to finish 2 dashboard redeploys (Vercel env var,
  Render CORS env var) and report back a CORS-free login/save test. No workaround —
  needs the user's own dashboard access, no API tokens available to automate either.
</blockers>

## Required Reading (in order)
1. `.planning/phases/02-cart-o-p-blico-no-ar/02-06-PLAN.md` — full plan text, all 3
   tasks. Read the whole file, but mentally substitute every `{dominio}`/`{D}`
   reference with `vcard-app-one.vercel.app` given the domain-deferral adaptation
   above, and skip/mark-deferred anything that's specifically about DNS/registro.br
   or the BRAND-01 TLS-on-custom-domain check.
2. `.planning/STATE.md` Blockers/Concerns — has the standing BRAND-01 deferral entry;
   don't duplicate it, just reference it.
3. `.planning/phases/02-cart-o-p-blico-no-ar/02-05-SUMMARY.md` — has the Render URL
   and the Dockerfile-Path-relative-to-Root-Directory pitfall (relevant context if
   the user needs to touch Render config again).

## Infrastructure State
- Render: `vcard-app-tihd` service live at `https://vcard-app-tihd.onrender.com`,
  `/health` returns 200/`database:up`, keep-alive cron active.
- Vercel: new project for `apps/web` deployed, URL `https://vcard-app-one.vercel.app`.
  `NEXT_PUBLIC_API_URL` set. `NEXT_PUBLIC_APP_URL` instructed but not yet confirmed
  set+redeployed. `BLOB_READ_WRITE_TOKEN` reused from Phase 1.
- Render `Cors__WebOrigin`: update to `https://vcard-app-one.vercel.app` instructed
  but not yet confirmed applied+redeployed.
- GitHub: repo now at `https://github.com/gabrielviol/vcard-app`, default branch
  renamed `master` -> `main` this session, `main` in sync with `origin/main` as of
  commit `fdaa6ce`.
- No custom domain registered (BRAND-01 still deferred, unchanged decision).

<context>
Executing Phase 2's final plan (02-06) via direct gsd-executor spawns per plan task,
same pattern as 02-05. Task 1 is a pure-dashboard checkpoint (Vercel + Render UI,
no code) that the orchestrating session walks the user through interactively —
no executor agent needed for it. Task 2 is `type="auto"` and should go through the
worktree-executor-merge-push pattern. Task 3 is another human-verify checkpoint
requiring live curl/browser testing against the real Vercel URL, walked through
interactively like plan 02-05's Task 3 was. This is a clean, non-broken pause —
nothing failed, we're just waiting on two dashboard clicks from the user.
</context>

<next_action>
Start with: ask the user whether the Vercel `NEXT_PUBLIC_APP_URL` redeploy and the
Render `Cors__WebOrigin` redeploy are both done, and whether login+save at
`https://vcard-app-one.vercel.app/login` works without a CORS error in the browser
console. If yes, close out Task 1 (no commit needed — it's dashboard-only, no files
changed), then spawn a gsd-executor (isolation="worktree") for Task 2 with the
domain-deferral adaptation spelled out in the prompt (see `<remaining_work>` above).
If the user reports a CORS error, the most likely cause is Render not having
redeployed after the env var save — suggest a manual "Deploy latest commit" on Render
before troubleshooting further.
</next_action>
